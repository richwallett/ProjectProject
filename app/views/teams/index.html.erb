<h1>All Teams</h1>

<ul id="teams">
  <% @teams.each do |team| %>
    <li>
      <%= link_to "#{team.name}", team_path(team) %>
    </li>
  <% end %>
  <%= link_to "Create a New Team", new_team_path, class: "new_team" %>
</ul>